# Monitor de Hardware 🖥️
Este es un proyecto de inicio para leer el estado físico del PC (CPU, RAM) utilizando Python en Windows 11. 

## Requisitos
- Python 3.x
- Librería `psutil` (instalar con `pip install psutil`)